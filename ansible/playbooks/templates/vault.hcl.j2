ui = true
disable_mlock = true

api_addr     = "http://{{ ansible_host }}:8200"
cluster_addr = "http://{{ ansible_host }}:8201"

storage "raft" {
  path    = "/opt/vault/data"
  node_id = "vault-1"
}

listener "tcp" {
  address     = "0.0.0.0:8200"
  tls_disable = true
}
